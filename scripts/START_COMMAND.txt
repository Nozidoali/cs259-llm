bash -c "cd /workspace && ([ ! -d cs259-llm ] && git clone https://github.com/Nozidoali/cs259-llm.git cs259-llm || true) && cd /workspace/cs259-llm && git fetch origin && git checkout alice/rmoe && python3 -m venv venv && source venv/bin/activate && pip install -q --upgrade pip && pip install -q -r requirements.txt && [ ! -f .env ] && [ -f .env.example ] && cp .env.example .env || true && export PYTHONUNBUFFERED=1 OMP_NUM_THREADS=1 TOKENIZERS_PARALLELISM=false && python train.py data/train_rmoe.json"
