# Environment configuration
# WORK_DIR: Working directory for models, data, and logs (optional, defaults to current directory)
# WORK_DIR=
# Optional: Override llama.cpp location (defaults to external/llama.cpp)
# LLAMA_CPP_DIR=/path/to/llama.cpp
